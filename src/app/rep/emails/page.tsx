import EmailInbox from '@/components/rep/email-inbox';

export default function RepEmailsPage() {
  return (
    <EmailInbox
      title="Emails"
      description="Customer inboxes — Amazon receipts, OTPs, order confirmations, and links customers send you."
    />
  );
}
